[phases.setup]
nixPkgs = ["nodejs-18_x", "nodePackages.pnpm", "ffmpeg", "python3"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pnpm install"]

[phases.build]
dependsOn = ["install"]
cmds = ["pnpm run build --if-present"]

[start]
cmd = "pnpm start"
